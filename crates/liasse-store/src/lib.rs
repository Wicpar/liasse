//! Store: the typed storage contract the runtime executes against — state,
//! history, blobs, and serial-ordering primitives. Implementations provide
//! the guarantees; the runtime provides the semantics. `liasse-pg` is the
//! PostgreSQL implementation.
