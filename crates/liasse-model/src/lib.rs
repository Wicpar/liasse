//! Model: parses spanned syntax into the validated semantic model — state
//! tree, keyed collections, views, mutations, surfaces, roles, modules,
//! buckets, meters, keyrings, and blob declarations (SPEC.md Parts I–III
//! static rules). A constructed model is proof the package is valid: parse,
//! don't validate.
